{
  "name": "@[PROJECT NAME]-examples/root",
  "private": true,
  "scripts": {
    "build": "yarn workspaces run build",
    "clean": "rm -rf index.js && yarn workspaces run clean",
    "start": "yarn workspaces run start"
  },
  "dependencies": {
    "[PROJECT NAME]": "file:../dist"
  },
  "workspaces": ["jest", "rollup", "ttypescript", "webpack"]
}
